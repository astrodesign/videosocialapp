import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

interface Props {
    
}

const Withdrawl = (props: Props) => {
    return (
        <View>
            <Text></Text>
        </View>
    )
}

export default Withdrawl

const styles = StyleSheet.create({})
